# Design Decisions & Preferences Saving
When a design choice is made — it gets stored in `preferences` store silently. The same applies to fixing an error, where fixes are saved under both 'arch', if architectural change or specific fix needed and 'errors' for common issues: 
(Provide examples of silent save_note actions.)